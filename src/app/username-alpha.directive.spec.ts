import { UsernameAlphaDirective } from './username-alpha.directive';

describe('UsernameAlphaDirective', () => {
  it('should create an instance', () => {
    const directive = new UsernameAlphaDirective();
    expect(directive).toBeTruthy();
  });
});
